# In development, the app uses Vite proxy (/api) to avoid ad blocker issues
# In production, uncomment and set your production API URL:
# VITE_API_BASE_URL=http://localhost:5001/api

# For development, leave this commented - Vite proxy will handle it
